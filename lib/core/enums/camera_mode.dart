enum CameraMode {
  photo,
  video,
  dual, // Front and back camera
}

enum CameraFlash {
  off,
  on,
  auto,
  torch,
}

enum CameraLens {
  front,
  back,
  wide,
}

enum EditTool {
  text,
  draw,
  crop,
  sticker,
  filter,
  none,
}